# Node.js

# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '14.x'
  displayName: 'Install Node.js'

- script:
    sudo npm install
  displayName: 'npm install in dev mode'

- script: 
    sudo npm install -g gulp
    sudo npm install -g nyc
  displayName: 'install gulp globally'

- script: 
    sudo npm audit fix
  displayName: 'npm audit and fix'

- task: gulp@1
  displayName: 'run tests'

- task: PublishTestResults@2
  condition: succeededOrFailed()
  inputs:
    testRunner: JUnit
    testResultsFiles: '**/TEST-RESULTS.xml'

- task: PublishCodeCoverageResults@1
  inputs: 
    codeCoverageTool: Cobertura # or JaCoCo
    summaryFileLocation: '$(System.DefaultWorkingDirectory)/**/*coverage.xml'
    reportDirectory: '$(System.DefaultWorkingDirectory)/**/coverage'

- task: CopyFiles@2
  inputs:
    SourceFolder: '$(System.DefaultWorkingDirectory)'
    Contents: |
      **\*.js
      package.json
    TargetFolder: '$(Build.ArtifactStagingDirectory)'

- task: PublishBuildArtifacts@1

- task: ArchiveFiles@2
  inputs:
    rootFolderOrFile: '$(System.DefaultWorkingDirectory)'
    includeRootFolder: false

- task: CmdLine@2 # Push changes to GitHub (substitute your repo)
  inputs:
    script: |
      git config --global user.email mikey.moritz@gmx.at
      git config --global user.name "DerSkillBaumApfel"
      git remote add swrepo https://github.com/DerSkillBaumApfel/SwDepLab2.git
      git add package.json -A
      git commit -m "Test Commit from Azure DevOps"
      git -c http.extraheader="AUTHORIZATION: basic ***" push swrepo HEAD:master
      
      
      


